# bytetrack_custom.yaml

tracker_type: bytetrack

track_high_thresh: 0.6
track_low_thresh: 0.1
new_track_thresh: 0.6

track_buffer: 90        # 🔥 keeps ID alive ~3 seconds @30fps
match_thresh: 0.85      # 🔥 more tolerant matching

fuse_score: true

# Motion model
motion:
  use_kalman: true
